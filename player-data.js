(function () {
  'use strict';

  // This public file intentionally contains no campaign handout text.
  // The keeper console sends one player-safe payload at a time after release.
  window.NG_PLAYER_DATA = Object.freeze({
    protocol: 'null-grail-player-v2',
    handouts: []
  });
}());
